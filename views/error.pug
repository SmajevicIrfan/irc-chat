extends layout

block additional
  style.
    body {
      padding: 50px;
      font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
    }

    a {
      color: #00B7FF;
    }

block content
  h1= message
  h2= error.status
  pre #{error.stack}
